import _ from 'lodash'
import {
    SimpleGrid,
    Box,
    Image,
    Heading,
    Avatar,
    Text,
    LinkBox,
    LinkOverlay,
    Stack,
    Button,
    Flex,
    useColorModeValue,
} from '@chakra-ui/react'

export default function Musician({ data }) {
    return (
        <>
            <p>salam</p>
            {/* { data.map((i) => i)} */}
        </>
    )
}
